@model IEnumerable<TheSchedulerProject.Models.Interest>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.weeklyGoal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.minTime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.priority)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.colorId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.weeklyGoal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.minTime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.priority)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.colorId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.interestId }) |
            @Html.ActionLink("Details", "Details", new { id=item.interestId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.interestId })
        </td>
    </tr>
}

</table>
